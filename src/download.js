chrome.downloads.download({
  url: "URL_DO_ARQUIVO",
  filename: "CAMINHO_COMPLETO_PARA_DESTINO/ARQUIVO.EXT",
});

chrome.downloads.onChanged.addListener(function (downloadItem) {

});

chrome.downloads.onChanged.addListener(function (downloadItem) {

});
